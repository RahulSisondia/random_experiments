PROJECT( sliding_window )
SET( sliding_window_SOURCES 
sliding_window.cpp)
ADD_EXECUTABLE( sliding_window ${sliding_window_SOURCES} )

PROJECT(arrays)
SET (arrays_SOURCES
arrays.cpp
)
ADD_EXECUTABLE(arrays ${arrays_SOURCES})