PROJECT( shared_ptr )
SET( SHARED_PTR_SOURCES
shared_ptr.cpp )

ADD_EXECUTABLE( shared_ptr ${SHARED_PTR_SOURCES} )

PROJECT( chrono )
SET( CHRONO_SOURCES
chrono.cpp )

ADD_EXECUTABLE( chrono ${CHRONO_SOURCES} )